....................................F.                                   [100%]
=================================== FAILURES ===================================
________________ test_protocol_and_source_derivation_reproduce _________________

    def test_protocol_and_source_derivation_reproduce():
        import json
        import io
        from zipfile import ZipFile
        data = n.protocol()
        assert data['tasks'] == n.TASKS
        for name, item in data['sources'].items():
            assert r.read(b.ROOT / item['path']) == n.derived_parts(name)
        for directory in sorted((n.BASE / 'captures').iterdir()):
>           assert n.evaluate(directory) == json.loads((directory / 'evaluation.json').read_text())
E           AssertionError: assert {'adapter': '...a341375', ...} == {'adapter': '...a341375', ...}
E             
E             Omitting 5 identical items, use -vv to show
E             Differing items:
E             {'cases': [{'actionable_findings': [], 'changed_raw_parts': ['[Content_Types].xml', '_rels/.rels', 'docProps/core.xml'...deletions: 2 -> 4 - added during editing (only a defect if ids collide, see REV001)', 'severity': 'info'}], ...}, ...]} != {'cases': [{'actionable_findings': [], 'changed_raw_parts': ['[Content_Types].xml', '_rels/.rels', 'docProps/core.xml'...deletions: 2 -> 4 - added during editing (only a defect if ids collide, see REV001)', 'severity': 'info'}], ...}, ...]}
E             Use -v to get more diff

tests/test_docx_boundaries.py:145: AssertionError
=========================== short test summary info ============================
FAILED tests/test_docx_boundaries.py::test_protocol_and_source_derivation_reproduce
1 failed, 37 passed in 0.64s
